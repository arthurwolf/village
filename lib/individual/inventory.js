// Libraries 
const fs = require('fs');

// The inventory is the part of the individual that holds objects.
// By default, without a bag to hold things, the inventory is very limited.
module.exports = class Inventory {
    
    constructor() {
    }

}